#include <stdio.h>
#include <stdlib.h>

struct Node{
    int data;
    struct Node *next;
}front = NULL, rear = NULL;

void create(){
    struct Node *p, *t;
    int x;
    Queue q;
}

int main(){

    return 0;
}